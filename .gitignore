# Apptainer images (large, built on the cluster)
*.sif

# Host-staged clone of the private nre repo (re-cloned by build_moco_sif.sh)
containers/.nre-src/

# Run artifacts
logs/
manifests/

# SLEAP prediction outputs
*.slp
*.predictions.slp

# Local overrides
config/*.local.sh
config/*.local.yaml

# Python / editor cruft
__pycache__/
.DS_Store
